// Compatibility wrapper.
// Some generated files import CrowdinLocale from `./constants`, while the source of truth
// lives in `./generated/constants`.

export type { CrowdinLocale } from './generated/constants';
